plugin.tx_themes {
	settings {
		themes.languages.available = {$themes.languages.available};
		themes.languages.current.uid = {$themes.languages.current.uid};
		themes.languages.default.isoCodeShort = {$themes.languages.default.isoCodeShort};
		themes.languages.default.label = {$themes.languages.default.label};
		themes.languages.default.flag = {$themes.languages.default.flag};
	}
}
